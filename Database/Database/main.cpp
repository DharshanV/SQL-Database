/* Author: Dharshan Vishwanatha
 * Project: Database
 * Purpose: Build a SQL-like database
			that can take in commands
			from the user such as 
			make table, select table,
			and insert into table. This
			project brings in all the
			previous projects, such as
			String Tokenizer, BPlusTree,
			Map, and Multi-Map to create
			a semi-sql.
 */
#include "SQL.h"

int main() {
	SQL sql;
	sql.run();
	return 0;
}